describe('Example Test Suite', () => {
  test('should pass - empty test to verify jest is working', () => {
    // This empty test should pass and confirm Jest is executing correctly
    expect(true).toBe(true);
  });
}); 