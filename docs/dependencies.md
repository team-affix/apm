# Dependencies (TODO)

This page will cover how dependencies work in APM.

Planned sections:

- Overview of direct vs transitive dependencies
- PackageTree and topological install order
- Peer dependency resolution
  - Name-based overrides
  - Detecting unresolved peers
  - Strategies and examples
- Project `deps.txt` format and best practices
- Registry `ls`, `getPackageTree`, and `getProjectTree` semantics
- Common pitfalls and troubleshooting

Status: TODO — to be filled out with detailed explanations and examples.
